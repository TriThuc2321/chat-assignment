import { lazy } from 'react';

export const Header = lazy(() => import('./Header'));
export const MessageItem = lazy(() => import('./MessageItem'));
export const InputMessage = lazy(() => import('./InputMessage'));
